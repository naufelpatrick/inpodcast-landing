SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
RESEND_API_KEY=
CONTACT_FROM_EMAIL=InPodcast <contato@inpodcast.com.br>
# Optional: require fresh catalog sources instead of the local fallback.
CATALOG_REQUIRE_FRESH=0
# Optional: deterministic offline builds using data/catalog.json.
CATALOG_OFFLINE=0
